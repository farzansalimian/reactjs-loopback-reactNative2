module.exports = function(Product) {
   /* Product.on('dataSourceAttached', function(obj){
        var create = Product.create;
        Product.create = function(data, done) {
            console.log(data);
            //var aaa  =data['product_files'][0]['rawFile'];

            
            //console.log(aaa);
            //Product.app.models.product_files.upload.call(Product.app.models.product_files.container, aaa, done);

            //delete data['product_files'];
            create.call(Product, data, done);
            
            
        };
      });*/
};